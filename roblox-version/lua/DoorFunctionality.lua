local DoorModule = require(game:GetService("ServerStorage"):WaitForChild("DoorModule"))
DoorModule.initDoorL(script.Parent) -- for doors with hinge on the left
DoorModule.initDoorR(script.Parent) -- for doors with hinge on the right
